# BusLine AutoStops 开发笔记

版本：v0.1.1（本地开发，**未发布**）  
最后更新：2026-09-25

## v0.1.1 实机日志结论（2026-09-25）

从 `Logs\Modding.log` / `BusLineAutoStops.log` 读到：

1. **UI 已注册但 `m_ModuleId:null`** —— `mod.json` 无 UTF-8 BOM（TTE 有 BOM）。已补 BOM。
2. **快捷键真的触发**，但全是 `拒绝进入：未选择站台资产` —— SetPrefab 钩子没跟上。已改为每帧轮询 `ToolSystem.selected` + `HasComponent<TransportStopData>`。
3. 设置页「关于」：版本 / 作者 yuexian / 三个外链按钮（照 AccessAnarchy）。

## 本轮结论

1. **功能骨架已落地**：懒人模式状态、快捷键/按钮入口、自动放置与删除联动账本、悬停虚影、UI 绑定、12 语言、退出删除策略。
2. **离线验证**：T3 纯逻辑 22 条全绿（`tests/t3/build-run.js`）；编译探针 0 错误（`scripts/compile-probe.js`，对游戏 Managed + Harmony 2.2.2）。
3. **BridgeTheLanguageGap：不改 BLG，本模组已按可翻译路径组织**（用户已拍板）。
   - 面板：`AddSource` + 前端 `<Localized id>` / `useLocalization`（必须走 Translate，禁止 TS 词典直出）。
   - key：`Options.…[setting.id.Panel.slug]` / `Common.ACTION[setting.id.Panel.slug]` → BLG **ModOptions**。
   - 选项行：框架 locale id → **ModOptions**。
   - 下拉枚举成员：`Options.<id>.EXITDELETEPOLICY[成员]` 的 `[]` 内是成员名 → BLG 归 **GameCore**（勾「游戏本体」可翻）；T3 已钉住。
   - 纯函数 `BlgKeys.cs` + T3 35 条（含 BLG 形状 12 条）。
4. **本地部署**：`node scripts/deploy-local.js` →  
   `F:\SteamLibrary\steamapps\common\Cities Skylines II\Cities Skylines II\Mods\BusLineAutoStops\`  
   （与 `CSII_LOCALMODSPATH` 为同一 junction）。布局对齐 TTE/ZoneSnapper：`BusLineAutoStops.dll` + `0Harmony.dll`(2.3.3) + `BusLineAutoStops.mjs` + `mod.json`。
5. **环境坑（本轮）**：本 shell 的 `dotnet restore` 全局报 `NuGet.targets path1 null` ⇒ 官方 `dotnet build` 未跑通，改用 `scripts/deploy-local.js`（Roslyn csc）部署。前端 `scripts/build-frontend.js`（ESM `.mjs`，与 TTE 相同拷到 DLL 旁）。

## 需求对照

| 需求 | 状态 |
|------|------|
| 快捷键 + 左上角按钮 | 入口/状态/UI 绑定已写；前端需 `npm i && npm run build` |
| 懒人模式进线路创建 | `RouteToolPatches.ActivateBusLineTool`（反射 SetActive，失败打日志） |
| 停靠点自动放站台 | `AutoPlaceSystem` 对 Created+Waypoint+BusStop |
| 预览虚影 / 红虚影 | `StopGhostSystem` 建 Temp.Create，规则交给原版校验 |
| 已有站台不放置 | 同类型 10m 内判定 |
| 删停靠点联动删站台 | 仅 `ByThisMod` |
| 完成线路保持开启 | 是（不自动关） |
| 仅公交 | `TransportKind` 架构预留，仅 Bus 启用 |
| 资产面板过滤 | **未实现**（需 ToolbarUISystem/目录过滤，后续） |
| 选项快捷键+下拉 | `Setting.cs` |
| 12 语言 | `LocaleTable.cs` |
| 面板可被 BLG 翻 | ✅ Localized/useLocalization + Options/ACTION key（ModOptions） |
| 关闭恢复原版 | 状态门闩；退出按策略清理 |
| 不改 BLG | 已遵守 |
| 不上传 | 已遵守 |

## 机制备忘

- 线路停靠点：`CreationDefinition` + `WaypointDefinition` → `GenerateWaypointsSystem` → `ApplyRoutesSystem`
- 站台：`ObjectToolSystem` 放置；`TransportStopData` / `RouteConnectionData`；`Connected` 吸附
- 虚影 = `Temp` + `TempFlags.Create`；违规由 `ValidationSystem`/`ErrorType` 上色
- BLG：`Scope.IsModIdentifier` 是 `ident.StartsWith(ModSetting.instances key)`；instances key ≈ `setting.id`（`程序集.命名空间.Mod类`）
- UI 挂载：`moduleRegistry.append("GameTopLeft", …)`（TTE 同款）

## 下一步

1. 修好 `dotnet restore` 后跑官方构建 + 部署 `Mods\BusLineAutoStops\`
2. 前端 `npm install && npm run build`，图标可再换成更接近原版线路工具
3. 实机 T4：进懒人模式、放 2 站、删 1 站、中途退出四种删除策略
4. 资产面板非公交站台置灰/隐藏（ToolbarUISystem）
5. 你拍板是否要改 BLG（推荐不改）

## 文件清单

- `BusLineAutoStopsMod.cs` `Setting.cs` `LocaleTable.cs` `LazyModeState.cs` `TransportKindRules.cs` `PlacedStopRegistry.cs`
- `Patches/` `Systems/` `Frontend/` `tests/t3/` `scripts/` `research/`
- `DESIGN.md` `README.md` 本笔记
